# Retrospective 2026-Q2

- **Quarter:** 2026-Q2
- **Compiled:** 2026-05-18
- **Compiled by:** human (no agent participation)

## Predictions reaching final status this quarter

No predictions have reached final status this quarter. All five open predictions
have windows ending in 2026-Q4 or later. First scoring opportunity: 2026-Q4
retrospective (compiled 2027-01-01).

| ID | Pattern | Substrate | Confidence | Outcome |
|----|---------|-----------|------------|---------|
| — | — | — | — | open |

## Calibration

No predictions resolved this quarter. Calibration analysis deferred to 2026-Q4.

## Pattern-level performance

No predictions resolved. Deferred.

## Framework changes applied this quarter

- **v0.2.0 → v0.3.0:** All 7 seed pattern files written and validated.
  Patterns were always part of the framework vocabulary (ARCHITECTURE.md §2)
  but had no dedicated files. Writing them surfaced two useful things:
  (1) the Indonesia/SEA cultural-variants requirement in TODO.md was not
  documented in ARCHITECTURE.md — it is now encoded in each pattern file;
  (2) the curiosity-past-the-fence and coercion-and-desperation patterns
  have no predictions yet, which is an honest gap worth tracking.

- **v0.3.0 → v0.4.0:** Signal ingestion implemented. Eight curated RSS/Atom
  sources verified live. Phylum Research Blog (original source #7) was found
  inactive after acquisition by Veracode; replaced with OpenSSF Blog.
  Risky Business RSS (original URL) returned 404; replaced with
  Seriously Risky Business Substack feed. CISA feed URL corrected from
  `/all/feed` to `/all.xml`. Three of eight initial URL guesses required
  correction — a reminder that RSS feed URLs are not stable.

- **v0.4.0 → v0.5.0:** Digest drafter implemented. First live run:
  8/8 sources, 187 signals, one API call, $0.16. Output truncated at 2048
  tokens (max_tokens ceiling too low); corrected to 4096. Hard guard encoded
  in system prompt with six explicit constraints and a required per-section
  disclaimer.

- **v0.5.0 → v0.6.0:** GitHub Actions wiring. Weekly digest workflow had
  incorrect flag names for `draft_digest.py` (`--signals` → `--signals-file`,
  `--output` removed in favour of stdout redirect). `notify_due.py` was
  referenced by `monthly-due.yml` but did not exist; written and tested.
  Monthly issue gate added: issue only opens if predictions are actually due
  (avoids noise in months where no windows have closed).

- **v0.6.0 first run:** GitHub Actions was not permitted to create pull
  requests by default (repo-level setting). Fixed by enabling
  `can_approve_pull_request_reviews` via the Actions workflow permissions API.
  First clean digest PR (#2, 2026-W21) opened successfully on retry.
  Monthly-due workflow ran correctly and created no issue (no predictions due
  in 2026-Q2, as expected).

## Open questions surfaced this quarter

- **Multi-pattern incidents:** The Fast16/Stuxnet retrospective analysis
  (week 21 digest) touched both ideology-faith-nation (state-directed sabotage)
  and craft-and-peer-recognition (academic publication of the technique).
  The framework currently assigns one pattern per prediction. Whether to allow
  multi-pattern predictions, or to require the human author to pick the primary
  motivation, is unresolved. ARCHITECTURE.md §9 notes this as an open question.

- **arXiv volume:** The cs.CR feed returns 40–50 entries per weekday. Capping
  at 50 entries per source means roughly one week of arXiv papers per digest.
  This is probably the right ratio, but it means fast-moving conference weeks
  (NeurIPS, CCS) will be undersampled. Consider a burst allowance for
  high-submission weeks in a future version.

- **Dedup scope:** Current dedup is per-signal-ID (URL or GUID). The same
  story covered by multiple sources (e.g., Krebs + THN + SANS) produces
  three separate signals with different IDs. The LLM clusters them naturally,
  but the raw signal count is inflated. A title-similarity dedup pass could
  reduce noise, at the cost of complexity. Deferred to v0.8.0 if it proves
  problematic in practice.

## Honest assessment

The practice survived the five-week manual phase without feeling empty.
Writing five predictions against seven patterns surfaced which patterns are
currently most activated (boredom-with-asymmetric-leverage, ideology-faith-nation)
and which have no predictions yet (curiosity-past-the-fence, coercion-and-desperation).
That asymmetry is informative: it reflects the author's current attention, not
the framework's limits, and is worth correcting in Q3.

The automation build (v0.4.0–v0.6.0) took one session and produced a working
pipeline. The first live digest (2026-W21) identified three candidate pattern
activations with honest uncertainty ratings. Whether those activations hold
over the next several weeks will be the first real test of whether the signal
sources are well-chosen.

The practice should continue. The risk at this stage is not that it becomes
useless — it's that the weekly digest becomes a box to check rather than a
thing to read. The checklist on the weekly PR is the guard against that.
